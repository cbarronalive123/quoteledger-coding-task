# Quick start — backend

```powershell
.\.venv\Scripts\Activate.ps1
cd backend
python manage.py runserver
```

Prerequisites: venv deps installed, `.env` with `DATABASE_URL`, and `python manage.py migrate` already run.

Full setup: [README.md](README.md) and the root project README.
